{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["node.js->node:22"]
    }
  },
  "deploy": {
    "startCommand": "npx openclaw@latest gateway --port ${PORT:-8080}"
  }
}
